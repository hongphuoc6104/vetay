/** Neutral integration/performance fixture; no private video assets or TTS required. */
import fs from 'node:fs/promises';import path from 'node:path';import {spawnSync} from 'node:child_process';
const root=path.resolve(import.meta.dirname,'../../..');const seconds=Number(process.argv[2]||30);if(!Number.isInteger(seconds)||seconds<10||seconds%10)throw Error('Use a multiple of ten seconds');const dir=path.join(root,'sys/work',`benchmark-${seconds}`);await fs.mkdir(dir,{recursive:true});
const scenes=Array.from({length:seconds/10},(_,i)=>{const start=i*10,end=start+10;return {id:'scene-'+i,start,end,role:i%3===0?'hook':'body',theme:'auto',title:['Một ý cần làm rõ.'],elements:[{id:'paper-'+i,type:'projection',width:880,height:650,zoom:1,rx:.1,ry:-.12,py:-.1,children:[{id:'image-'+i,type:'image',src:'/sys/templates/brand/logo-light.png',x:35,y:40,width:280,height:280},{id:'caption-'+i,type:'text',text:'Thông tin có liên hệ.',x:340,y:100,width:500,fontSize:48,color:'paper'},{id:'pen-'+i,type:'stroke',points:[[340,270],[750,270],[750,500],[150,500]],color:'gold',animate:{progress:[{at:start,value:0},{at:end-.3,value:1}]}}]}],camera:{zoom:[{at:start,value:1},{at:start+2,value:1.025},{at:end,value:1.025}]}};});
const p={id:'benchmark-'+seconds,title:'Neutral performance fixture',stylePreset:'net-cinematic-v1',rendererVersion:'1.0.0',palette:'technology',format:{width:1080,height:1920,fps:30},approved:true,audioMaster:'master.wav',scenes};await fs.writeFile(dir+'/project.json',JSON.stringify(p,null,2));await fs.writeFile(dir+'/timeline.json',JSON.stringify({targetSeconds:seconds,phrases:[],valid:true},null,2));const result=spawnSync('ffmpeg',['-y','-v','error','-f','lavfi','-i','anullsrc=r=48000:cl=mono','-t',String(seconds),'-c:a','pcm_s16le',dir+'/master.wav']);if(result.status)throw Error('Fixture audio failed');console.log(dir);
