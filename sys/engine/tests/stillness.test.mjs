import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';import path from 'node:path';import {spawnSync} from 'node:child_process';import {contentHolds} from '../visual/stillness.mjs';
test('caption-only motion does not hide an unchanged teaching region',async()=>{const dir=path.resolve('sys/cache/stillness-tests');await fs.mkdir(dir,{recursive:true});const file=dir+'/caption-only.mp4';const p=spawnSync('ffmpeg',['-y','-v','error','-f','lavfi','-i','color=c=white:s=1080x1920:r=30:d=9','-vf',"drawbox=x=100:y=1720:w=800:h=100:color=black:t=fill:enable='lt(mod(t,1),0.5)'",'-c:v','libx264','-preset','ultrafast','-pix_fmt','yuv420p',file]);assert.equal(p.status,0);const holds=await contentHolds(file);assert.ok(holds.some(h=>h.end-h.start>8));});
